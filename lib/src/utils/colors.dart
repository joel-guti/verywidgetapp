import 'package:flutter/material.dart';

class VeryWidgetColors {
  static const Color principalcolor = Colors.blue;
  static const Color blackColor = Colors.black;

  static const shadow =
      Shadow(offset: Offset(0, 2), color: VeryWidgetColors.blackColor);
}
